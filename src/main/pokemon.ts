export class Pokemon {
    name: string;
    speed: number;
    category: string;

    constructor(name: string, speed: number, category: string) {
        this.name = name;
        this.speed = speed;
        this.category = category;
    }
}